name: CI
on:
  push:
    branches:
      - '**'
    tags: ['*']
  pull_request:
  schedule:
    - cron: '0 0 1 * *'  # Runs at midnight UTC on the 1st of every month
  workflow_dispatch:       # Allows manual trigger from the Actions tab
concurrency:
  # Skip intermediate builds: always.
  # Cancel intermediate builds: only if it is a pull request build.
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ startsWith(github.ref, 'refs/pull/') }}
jobs:
  test:
    name: Julia ${{ matrix.version }} - ${{ matrix.os }} - ${{ github.event_name }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        version:
          - '1.10'
          - '1.11'
          - '1.12'
          - 'pre'
        os:
          - ubuntu-latest
          - macOS-latest
          - windows-latest
    steps:
      - uses: actions/checkout@v6
      # Omit `arch` so setup-julia uses each runner's native architecture: x64 on
      # Ubuntu/Windows, aarch64 on the Apple Silicon macOS runners.
      - uses: julia-actions/setup-julia@v3
        with:
          version: ${{ matrix.version }}
      - uses: julia-actions/cache@v3
      - uses: julia-actions/julia-buildpkg@v1
      # Fathom is an unregistered test dependency (used for plot theming).
      - name: Check out and develop Fathom
        shell: bash
        env:
          FATHOM_PATH: ${{ runner.temp }}/Fathom
        run: |
          git clone --depth 1 https://github.com/brendanjohnharris/Fathom.jl "$FATHOM_PATH"
          julia --color=yes --project=test -e 'using Pkg; Pkg.develop([PackageSpec(path = pwd()), PackageSpec(path = ENV["FATHOM_PATH"])])'
      - uses: julia-actions/julia-runtest@v1
      - uses: julia-actions/julia-processcoverage@v1
      - uses: codecov/codecov-action@v6
        with:
          files: lcov.info
        env:
          CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
